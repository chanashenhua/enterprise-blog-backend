# MinIO Buckets

The local development environment uses one bucket:

- `blog-files`: stores article images and attachments.

Files must be accessed through `file-service` in later tasks. Do not expose unrestricted public MinIO URLs from application code.
