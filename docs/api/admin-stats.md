# 管理端互动概览接口

管理员可通过以下接口查看全站互动汇总：

```http
GET /api/admin/stats/overview?limit=10
```

调用者必须具有 `ADMIN` 角色。`limit` 控制热门文章数量，范围 1～50。

返回内容包括：

- 去重浏览总数；
- 当前点赞总数；
- 当前收藏总数；
- 产生过互动的文章数量；
- 产生过互动的员工数量；
- 按浏览、点赞、收藏排序的热门文章列表。

统计结果直接从 PostgreSQL 的互动事实表聚合，不维护额外缓存计数，避免概览与文章详情计数不一致。
