# 内容运营概览接口

`GET /api/admin/content/overview` 仅允许 `ADMIN` 角色访问，用于管理首页展示内容沉淀与专题整理的基础指标。

返回字段包括：

- 非删除文章总数，以及已发布、草稿、待审核、已撤回数量；
- 已发布文章中的分类覆盖数和标签覆盖数；
- 专题数量、专题文章关系数量和专题创建者数量。

该接口只读取文章服务本地数据库，不调用搜索、统计或通知服务。管理前端会与互动、评论、订阅、通知概览并行加载；任一服务暂时不可用时，其余指标仍可展示。
